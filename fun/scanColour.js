/* var Rejson = require('iorejson');
for(var i in x){  // For # of index in array 
    if(i === black)        // If Colour is black 
    document.write(x[i])  // Write out the UID for the business if colour is black
} */
for (i in [x], I <= X, i++) // i = index in array x = array total 
if(colour === black){
UID written out 
}
